#include "monty.h"
/**
 * stack - Prints the top.
 * @head: Top of the stack.
 * @number: line_number
 * Return: no return
*/
void stack(stack_t **head, unsigned int number)
{
	(void)head;
	(void)number;
	bus.lifi = 0;
}
